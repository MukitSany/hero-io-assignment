import React, { use } from 'react';

const App = ({singleApp}) => {
    // const data = use(appPromise);
    // console.log(data);
    console.log(singleApp);

    return (
        <div>
            <h1>Single Book</h1>   
        </div>
    );
};

export default App; 